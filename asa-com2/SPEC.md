# あさちゃんコミュニケーションボード ＜Asa-Com2＞ 仕様書（手書き入力版）

| 項目 | 内容 |
| --- | --- |
| アプリ名 | あさちゃんコミュニケーションボード 手書き版（Asa-Com2） |
| 版数 | **Rev.3（2026-09-22 実装完了版）** |
| 作成日 | 2026-09-22 |
| 想定プラットフォーム | iPad（Safari / ホーム画面へ追加によるフルスクリーン起動） |
| 実装形態 | HTML5 単一ファイル（`asa-com2/index.html`）／外部依存なし・オフライン動作 |
| 公開URL（予定） | https://furuta-ideas.github.io/kashiwanoha-tour-guide-faq/asa-com2/ |
| デザインシステム | Google Material Design 3（M3）準拠。Asa-Com（選択版）と同じトークン・同じ画面構成 |
| 関連文書 | `asa-com/SPEC.md`（Asa-Com 選択版 Rev.3） |

> 本書は **Asa-Com（選択版）とは別アプリ**としての第1版仕様です。将来的に1つのアプリへ統合し、
> 設定で「選ぶモード／書くモード」を切り替える想定ですが、本版では手書き入力のみを対象とします。

### Rev.2 → Rev.3 の変更点（「本人は目がほとんど見えない」前提の反映）

| # | 変更内容 |
| --- | --- |
| A | **濁点・半濁点を手書きで入力**する（ボタンは介助者用の予備に降格）。文字のあとに続けて小さく「゛」「゜」を書くと、直前の文字が濁音・半濁音になる |
| B | 読み取れなかったときは女性の声で「**認識できません。**」と発話する |
| C | **左ペイン全体を書く面**にし、補助キーは**右（介助者の列）へ移動**。本人が書いている最中にボタンへ触れて誤動作することが無いようにした |
| D | **候補チップも書く面の外（介助者の列）へ移動**。本人の指が候補ボタンに当たって筆跡が途切れないようにした |
| E | 耳だけで状態が分かるよう**音の設計**を追加（指が触れた合図／読めた／あやしい／消した／読めない／文の確定で、それぞれ違う音） |
| F | 起動時に「**じゅんびが できました。かいて ください。**」と発話して、音が出ることを本人が確認できるようにした |
| G | 小さな点ひとつは「濁点の1画目」「ン・シの点」の可能性があるため、**少し待ってから**（書き足し待ちの2倍・最低700ms）句点／半濁点と判断する |

### Rev.1 → Rev.2 の変更点（ご回答の反映）

| # | 変更内容 |
| --- | --- |
| 1 | **小書き（ャュョッ）・空白は仕様から削除**（シンプル化のため当面対象外） |
| 2 | **「。」を文章の終了文字**とする。入力した時点で、先頭から「。」までを**自然な日本語として発話**し、同時に**ログエリアへ記載**、入力中の行は**全消去**する |
| 3 | 入力中の行には、**手書きの筆跡を行の高さに縮小した絵**を並べ、その**右下に認識した文字を小さく**表示する（手書きと認識結果をペアで見せて介助者の理解を助ける） |
| 4 | 補助キーの **［ぜんぶけす］を削除**（「。」で確定・消去されるため） |
| 5 | 濁音・半濁音は **［゛］［゜］ボタン**で直前の文字を変換する方式で継続 |
| 6 | 長いジグザグでの全削除は**不採用**。ジグザグは**1文字削除のみ** |
| 7 | 認識は**完全自作・オフライン方式**で実装。精度が不足する場合は外部ライブラリの採用を再検討（第10章 テスト後の確認事項） |

---

## 1. 目的と背景

発話が困難で、**指先で画面に文字を書くことはできる**利用者（以下「本人」）が、
iPad の画面に **1文字ずつ指で文字を書く**ことで意思を伝えるためのアプリ。

- 本人は「書く」→ アプリが読み取る → **その場で1文字ずつ声になる**
- 「。」を書くと、そこまでの文が**ひとつづきの日本語**として読み上げられ、ログに残る
- 「はい／いいえ」では伝えきれない言葉（固有名詞・数字・要望）を伝えられる

### 1.1 設計上の基本方針

1. **書いたらすぐ声になる** — カタカナ／数字と判断できた時点で即座に発音する（1文字ごと）
2. **待たせない** — 1文字ごとの確定ボタンは置かない。書き終わりを自動で判定する
3. **間違えてもすぐ戻せる** — ジグザグを描けば1文字消える（音声で「削除」と知らせる）
4. **耳だけで完結する** — 本人は画面がほとんど見えない。読めた／読めない／消した／文が伝わった、を
   すべて**声と音**で知らせ、本人が触る面にはボタンを一切置かない
5. **見せて安心させる** — 手書きの筆跡と認識結果を並べて表示し、**介助者**が誤認識に気づけるようにする
6. **単一画面で完結** — モーダルやページ遷移を最小化する
7. **ネットワーク不要** — 文字認識・音声合成ともに端末内で完結（オフライン動作）
8. **Asa-Com と同じ顔** — ヘッダー・ログ欄・コントロールバー・設定シートの構成を踏襲する

### 1.2 入力できる文字（本版の範囲）

| 種別 | 文字 | 入力方法 |
| --- | --- | --- |
| カタカナ 清音 | ア〜ン（46字） | 手書き |
| 長音符 | ー | 手書き（横1画）／［ー］ボタン |
| 算用数字 | 0〜9 | 手書き |
| 濁音・半濁音 | ガ〜ポ | **手書き**（直前の文字に続けて小さく「゛」「゜」を書く）／介助者用ボタン |
| 句点 | 。 | **手書き**（小さな丸・点）／介助者用ボタン |

- ひらがな・漢字・アルファベット・小書き（ャュョッ）・空白・読点（、）は**本版では対象外**
- カタカナと数字で字形が似るもの（`ロ`/`0`、`ニ`/`=`、`ク`/`タ`/`7` 等）は第4章の方法で判別する

---

## 2. 利用者と役割

| 役割 | 説明 | 主な操作 |
| --- | --- | --- |
| 本人（あさちゃん） | 意思を伝える人 | 文字を書く／ジグザグで消す／「。」で伝える |
| 介助者・家族 | 見守る人、設定する人 | 読み上げ・ログのON/OFF、設定、ログの保存・書き出し |

---

## 3. 画面レイアウト

添付の画面設計図（`docs/screen-sketch.jpg`）に対応する。

### 3.1 全体構成（横向きを基本）

```
┌──────────────────────────────────────────────────────┬────────────┐
│ Asa-Com2                                          ⚙  │            │
├──────────────────────────────────────────────────────┤  10:15     │
│ ┌───┐┌───┐┌───┐┌───┐┌───┐                      ▶よみ │  オチャ     │
│ │ ｱ ｱ││ ｲ ｲ││ ｳ ｳ││ ｴ ｴ││ ｵ ｵ│   （入力中の行）    │  ノミタイ。 │
│ └───┘└───┘└───┘└───┘└───┘                        あげ│            │
├──────────────────────────────────────────────────────┤  10:17     │
│                    ┊                                 │  ハイ。     │
│              ┄┄┄┄┄┼┄┄┄┄┄┄                            │            │
│                  力┊     （手書きキャンバス）        │  ログエリア │
│                    ┊                                 │   （2/7）  │
│                    ┊                                 │［ソ］［ア］ │
│         （左ペインは全部が書く面。ボタンは置かない）  │ 介助者用    │
│                    ┊                                 │゛ ゜ ー     │
├──────────────────────────────────────────────────────┤。 けす よみ │
│              （画面幅の 5/7）                        │ 🔊ON 📝ON  │
└──────────────────────────────────────────────────────┴────────────┘
```

| 領域 | 大きさ | 内容 |
| --- | --- | --- |
| ヘッダー | 高さ 56px | 「Asa-Com2」＋右端に設定（歯車、直径52px） |
| 入力中の行 | 高さ 124px（文字サイズ設定で追随） | 手書き筆跡＋認識文字のペアを横に並べる |
| 手書きキャンバス | **左エリアの残り全部** | 書く面。点線の十字ガイド＋実線の外枠。**ボタンは一切置かない** |
| ログエリア | 画面幅の **2/7** | 確定した文の履歴（新しいものが下、自動スクロール） |
| 候補チップ | ログエリア下 | 第2・第3候補（**介助者**が見て直すためのもの） |
| 介助者用ボタン | 候補チップの下 | ［゛］［゜］［ー］［。］［けす］［よみあげ］（3列×2段） |
| コントロールバー | 右列の下端 | 読み上げON/OFF、ログON/OFF のトグル（直径64px） |

- CSS Grid で `grid-template-columns: 5fr 2fr;`（設定で 3:1 / 3:2 / かくす に変更可）
- 「かくす」にしてもログ欄が消えるだけで、**介助者用ボタンの細い列は残る**
- 縦向き時は 上（入力）・下（ログ）の縦積みへ自動切替
- **本人が触る左ペインにはボタンが1つも無い**ため、書いている途中で誤ってボタンを押すことがない

### 3.2 入力中の行（手書き＋認識文字のペア表示）

| 項目 | 仕様 |
| --- | --- |
| 1文字の表示 | **枠（角丸12px）の中に、書いた筆跡を行の高さに合わせて縮小表示**し、その**右下に認識した文字を小さく**（高さの約28%）重ねる |
| 筆跡の描画 | 確定時の筆跡データ（正規化済み）をインライン SVG のポリラインで描画。線は `--on-surface`、太さは表示サイズに比例 |
| 筆跡の無い文字 | 介助者用ボタンで入れた文字は筆跡が無いため、枠の中央に文字そのものを大きく表示する |
| 濁点変換後 | 筆跡はそのまま、右下の認識文字だけが「カ」→「ガ」に変わる |
| 直前の1文字 | 確定直後 600ms は枠を淡い青でハイライト |
| あやしい認識 | スコアが低い文字は右下の認識文字を**黄色の下線**付きで表示 |
| 並べ方 | 左から順、末尾が常に見えるよう自動スクロール（横スクロール可） |
| 空のとき | 「ここに かいた もじが でます」を淡色で表示 |
| よみあげ | 右列の［よみあげ］を押すと現在の行全体を読み上げる（確定はしない） |

### 3.3 手書きキャンバス

| 項目 | 仕様 |
| --- | --- |
| マス | **1マス（1文字ずつ）**。左ペインいっぱい。中央に点線の十字ガイド、外周に実線の枠（ガイドは介助者向けの目安で、本人はどこに書いても・どんな大きさでもよい） |
| 実装 | `<canvas>`（`devicePixelRatio` 対応）。線幅は設定（細8 / 中12 / 太16px）、丸端、色 `--on-surface` |
| 入力イベント | Pointer Events（`pointerdown/move/up/cancel`）、`touch-action:none` |
| マルチタッチ | **最初の1点のみ**を筆として扱い、他の接触は無視（手のひら対策） |
| はみ出し | マス外へ出ても描画は続行（正規化で吸収） |
| 確定時 | 筆跡をフェードアウト（180ms）して空にする |
| 書き直し | ジグザグで、書きかけの筆跡があればそれを破棄、無ければ確定済みの1文字を削除 |
| 位置・大きさ | 正規化で吸収するため、マスのどこに・どの大きさで書いても認識できる |
| 字の大きさの学習 | 確定した文字の大きさを指数移動平均で覚え、「小さく書かれた記号（濁点・句点）」の判定基準にする |

### 3.4 介助者用ボタン（右列）

本人は画面が見えないため、これらは**介助者の予備手段**である。本人の操作はすべて手書きで完結する。

| キー | 動作 | 発音 |
| --- | --- | --- |
| ［゛ にごる］ | 直前の1文字を濁音に変換（カ→ガ）。もう一度押すと戻る。不可なら淡色・無効 | 変換後の文字 |
| ［゜ ぱぴぷ］ | 直前の1文字を半濁音に変換（ハ→パ）。もう一度押すと戻る | 変換後の文字 |
| ［ー のばす］ | 長音符を追加 | 「のばす」 |
| ［。 ぶんの おわり］ | **文の終了**（第6章） | 文全体を読み上げ |
| ［けす 1もじ］ | 1文字削除（ジグザグと同じ） | 「削除」 |
| ［よみあげ いまの行］ | 入力中の行を読み上げ（確定はしない） | 行の文字列 |

- 高さ 52px、右列に 3列×2段で配置。**本人が書く面からは完全に分離**されている
- 設定「介助者用ボタンを表示」OFF で隠せる（本人の手書きだけで全機能が使える）

### 3.5 ログエリア／コントロールバー

Asa-Com（選択版）4.4 / 4.5 と同一。ただし：

| 項目 | Asa-Com2 での扱い |
| --- | --- |
| 記録単位 | **「。」で確定した文（1行）単位**（1文字ごとには記録しない） |
| 種別 | `text`（手書き入力）のみ。音声入力（🎤）は本版では非搭載 |
| コントロールバー | マイクトグルの代わりに **読み上げ ON/OFF**。ログトグルは同じ |
| 左ボーダー | Google Blue（4px） |

---

## 4. 文字認識

### 4.1 認識のタイミング（最重要）

「書き終えた瞬間に声になる」ことを最優先し、**画数の確定を待たずに**判定する。

```
1画書く → 書き終わり（pointerup）
   ├─ ジグザグ判定 → 該当すれば即「削除」（4.5）
   ├─ 小さな記号の判定（4.4）
   │    ├─ 小さな2点＝濁点 かつ 直前が濁れる文字 → すぐ濁音に変換
   │    └─ 小さな丸・点 → すぐには決めず「記号待ち」（書き足し待ちの2倍・最低700ms）
   ├─ 認識を実行（いまの画数の筆跡すべてを使う）
   │    ├─ ① 1位候補のスコアが「即確定しきい値」以上
   │    │   かつ この画数で完結する字形しか無い（書き足しの見込みが無い） → その場で確定
   │    └─ ② それ以外 → 「書き足し待ち」タイマーを開始（既定 400ms）
   └─ 待ち時間内に次の1画が始まれば → タイマー解除して書き足しを継続
       待ち時間が経過したら       → その時点の1位候補で確定
```

| 設定 | 選択肢 | 既定 |
| --- | --- | --- |
| 書き足し待ち時間 | 200 / 300 / 400 / 600 / 800 / 1000 ms | **400ms** |
| 早く確定する（即確定） | ON / OFF（OFFなら常に待ち時間で確定） | ON |
| 読み取りのきびしさ | ゆるい / ふつう / きびしい | ふつう |

- **書き足しリカバリ**：即確定した直後 **700ms 以内**に新しい画が始まった場合、直前の確定を取り消して
  書き足しとして扱う（例：`ノ` を確定した直後に2画目を書いて `メ` にする）。取り消し時は読み上げ中の
  音声を止め、入力中の行からも削除する。
- 確定したらキャンバスの筆跡は消える（3.3）。

### 4.2 認識エンジン（外部依存なし・オフライン）

| 項目 | 仕様 |
| --- | --- |
| 方式 | **画数つき点群マッチング**（$P / $N 系の改良）＋ 画の方向・始点終点の特徴 |
| 前処理 | ①各画を等間隔に再サンプリング（1画 20点） ②全画まとめて重心を原点へ ③最大辺で正規化（**縦横比は保持** — `ー` と `1` を区別するため） |
| 照合 | 点群距離（画どうしの対応付け）＋ 各画の方向ベクトルの一致度（重み 0.35） |
| 筆順のゆれ | 画の対応は「書いた順どおり」を優先しつつ、**順不同の対応**も試して良い方を採用（順不同にはペナルティ） |
| 筆の向きのゆれ | 画の**逆向き**も試す（ペナルティあり。`ソ`と`ン`の取り違えを防ぐため小さくはしない） |
| テンプレート | カタカナ46字＋`ー`＋数字10字について、標準字形を **1字あたり1〜3種**（続け字・略字・画数違いを含む）内蔵 |
| 出力 | 上位3候補（文字＋スコア 0〜1） |
| 実装 | 100% JavaScript、モデルファイル無し、CDN 無し（`index.html` にインライン） |

#### 4.2.1 まぎらわしい字への対策

| 組 | 判別の手がかり |
| --- | --- |
| `ロ` / `0` | `ロ` は角ばった2〜3画、`0` は1画の丸 |
| `ソ` / `ン` | 画の**向き**（`ソ`＝右上から左下へ、`ン`＝左下から右上へ）と点の角度 |
| `シ` / `ツ` | 点の位置（`シ`＝左に縦並び、`ツ`＝上に横並び）と3画目の向き |
| `ク` / `タ` / `7` | 画数（2/3/1）と内部の点の有無 |
| `ス` / `フ` / `7` | 画数と折り返しの位置 |
| `1` / `ー` | 主方向（縦／横）＝正規化で縦横比を保持しているため区別できる |
| `エ` / `工` | 同字形。カタカナ `エ` として扱う |

- 設定「**数字をつかう**」OFF で数字テンプレートを候補から外す（カタカナだけの会話での精度向上）
- 設定「**数字だけモード**」ON で数字（と`ー`）だけを候補にする（電話番号・時刻の入力時）

### 4.3 候補の表示と訂正

| 項目 | 仕様 |
| --- | --- |
| 候補バー | **右列（介助者の側）**に、第2・第3候補を最大2つ表示。**書く面には置かない**（本人の指が当たって筆跡が途切れるため） |
| 表示時間 | 直前の確定から **4秒**（設定で 出さない / 3 / 4 / 5 / 10秒） |
| 操作 | チップをタップ → 直前の1文字を差し替え、差し替えた文字を発音 |
| 自信が低いとき | 1位スコアがしきい値未満でも確定はするが、入力中の行の認識文字に**黄色の下線**を付ける |
| 認識できないとき | **女性の声で「認識できません。」と発話**し、低い音を鳴らし、キャンバスを揺らして（120ms）筆跡を消す。画面には「認識できません」を1.5秒表示（設定で発話をOFFにできる） |

### 4.4 小さく書かれた記号の判定（濁点・半濁点・句点）

本人は画面が見えず、ボタンを押すことができないため、**濁点・半濁点・句点はすべて手書き**で入力する。
判定は「**直前に書いた文字より、うんと小さいかどうか**」で行う。大きさの基準は、直前までに確定した
文字の大きさ（指数移動平均）を使うので、本人の字の大きさに自動で追随する（履歴が無い間はマス短辺の24%）。

| 項目 | 仕様 |
| --- | --- |
| 記号とみなす大きさ | 外接矩形の長辺が、覚えている**字の大きさの 42% 未満**（かつ線の長さが字の大きさの1.3倍未満） |
| **濁点（゛）** | ①小さな**2画** または ②小さな**1画で縦か横に2回以上折り返した形**（2つの点を続けて書いたとき）。直前の文字が濁音にできるときだけ成立 → **すぐに**濁音へ変換し、変換後の文字を発音 |
| **半濁点（゜）／句点（。）** | 小さな**1画の丸・点**。どちらも同じ形なので、**直前の文字で決める**<br>・直前が ハヒフヘホ → **半濁点**（パピプペポ）<br>・それ以外 → **句点**（文の確定） |
| 丸の待ち時間 | 小さな丸・点は「濁点の1画目」「`ン` `シ` `ツ` `ソ` の点」の可能性があるため、**書き足し待ちの2倍（最低700ms）待ってから**判断する。その間に2つ目の点が来れば濁点になる |
| 「パ」にしたくなかったとき | 半濁点に変換した直後（**4秒以内**）に**もう一度小さな丸**を書くと、半濁点を取り消して（パ→ハ）**句点として文を確定**する。耳で「パ」と聞こえたら丸をもう一度書けばよい |
| 細長い小さな線 | 縦横比が 2.5 を超える（または 0.4 未満）ものは記号とみなさず、文字として認識する（小さめに書いた `ー` や `ノ` を守るため） |
| 3画以上 | 記号とはみなさない |

### 4.5 削除ジェスチャ（ジグザグ）

| 項目 | 仕様 |
| --- | --- |
| 判定 | **1画のみ**で、①x方向の折り返しが **3回以上** ②総移動距離がマス短辺の 1.2倍以上 ③各折り返しの振幅がマス幅の 12% 以上 ④主方向が横（横移動 ≧ 縦移動×0.8） |
| タイミング | その画を書き終えた瞬間（`pointerup`）。**書き足し待ちより優先** |
| 動作 | ①書きかけの筆跡があれば破棄（＝書き直し）／無ければ入力中の行の末尾1文字を削除 ②**「削除」と発音** ③削除した枠を 300ms 赤く表示してから消す |
| 行が空のとき | 「けすものが ありません」と発音（設定でOFF可）。ログは消さない |
| 誤爆対策 | `シ` `ツ` `ミ` `サ` 等と競合しないよう **1画かつ折り返し3回以上**に限定。しきい値は設定で ゆるい／ふつう／きびしい |
| 全消去 | **不採用**（「。」で行は空になるため） |
| 走り書き | 大きく塗りつぶすような走り書きも、折り返しが3回以上あれば削除として扱う |

---

## 5. 音声出力

| 項目 | 仕様 |
| --- | --- |
| エンジン | Web Speech Synthesis API（`speechSynthesis`）。オフラインで動作 |
| 言語 | `ja-JP` |
| 声 | **女性の声**を優先自動選択（`Kyoko` / `O-ren` / 名前に `Female` を含む ja-JP ボイス → 無ければ ja-JP の先頭）。設定で手動選択も可 |
| 1文字の発音 | 確定した瞬間に、その1文字だけを読み上げる（例：「ア」） |
| 連続入力時 | 取りこぼさないよう **キューへ積む**（設定「はやく書いたら前の声を止める」でキャンセル方式にも変更可） |
| 1文字の読み辞書 | 単独では不自然に読まれる字を補正：`ー`→「ノバス」、`。`→「マル」 |
| 数字 | 1文字ずつ「イチ」「ニ」…（`0`＝「ゼロ」、`7`＝「ナナ」）。文として読むときは数字のまま渡す |
| 削除 | 「削除」 |
| **読めなかったとき** | 「**認識できません。**」（設定でOFF可） |
| 消すものが無いとき | 「けすものが ありません。」（設定でOFF可） |
| 空のまま「。」 | 「まだ なにも かいて いません。」 |
| 濁点・半濁点 | 変換後の文字（「ガ」「パ」） |
| 起動時 | 「**じゅんびが できました。かいて ください。**」（音が出ることを本人が耳で確認できる） |
| **文の読み上げ** | 「。」の入力時、**行全体をそのまま1つの文として読み上げる**（1文字ずつではなく自然な日本語として）。末尾に「。」を付けて渡し、文末のイントネーションを自然にする |
| よみあげボタン | 現在の行を同じ方法で読み上げる（確定はしない） |
| 速度・高さ | 速度：遅い(0.85) / ふつう(1.0) / 速い(1.2)、高さ：低い(0.9) / ふつう(1.05) / 高い(1.2) |
| 読み上げOFF時 | 認識・表示・ログは動くが音声は出さない |
| iOS の制限 | 初回のユーザー操作までは音声が鳴らないため、起動後に1度だけ「がめんを さわって はじめる」オーバーレイを出す（画面全体がボタンなので、どこを触っても始められる） |

### 5.1 効果音（耳だけで状態が分かるようにする）

本人は画面が見えないため、声の前後に**短い音**で状態を知らせる。いずれも設定「効果音」でOFFにできる。

| できごと | 音 |
| --- | --- |
| 指が画面に触れた | とても小さな高い音（1400Hz・30ms）※設定「指が触れたら小さな音」 |
| 1文字読めた | 高い音（880Hz） |
| 読めたが自信が低い | 中くらいの音を2つ（560→470Hz）＝「あとで確かめて」の合図 |
| 1文字消した | 低い音（300Hz）＋「削除」 |
| 読めなかった | いちばん低い音（220Hz）＋「認識できません。」 |
| 文が確定した | 上がる2音（660→990Hz）＋ 文の読み上げ |

---

## 6. 文の確定（「。」）とログ

| 項目 | 仕様 |
| --- | --- |
| 確定の合図 | **「。」の入力**（手書きの小さな丸／介助者用の［。］ボタン）だけ。ほかに確定操作は無い |
| 確定時の動作 | ①行の末尾に「。」を付ける ②**行全体を自然な日本語として読み上げる** ③**ログへ1件追加**（読み上げと同時） ④**入力中の行を全消去**（筆跡ペアもすべて消える） |
| 行が空のとき | 「。」だけでは確定しない。「まだ なにも かいて いません。」と発話（誤操作防止） |
| 記録内容 | 日時（ISO 8601）、表示時刻（HH:MM）、種別（`text`）、本文（「。」を含む） |
| 保存先 | `localStorage`（キー：`asacom2.log`）。表示は直近 200 件 |
| ログOFF時 | 表示せず・保存せず・書き出し対象にもしない（読み上げは行う） |
| 書き出し | 設定から CSV（`asacom2_log_YYYYMMDD.csv`） |
| 消去 | 設定から「ログを消去」（確認あり） |

#### CSV フォーマット

```csv
datetime,time,type,text
2026-09-22T10:15:32+09:00,10:15,text,オチャ ノミタイ。
2026-09-22T10:19:41+09:00,10:19,text,テレビ 7。
```

---

## 7. 設定メニュー

ヘッダー右端の歯車から開く（「設定ボタンを長押しで保護」ON で1秒長押しが必要になる）。

#### 書く・読み取る

| 設定項目 | 選択肢 | 既定 |
| --- | --- | --- |
| 書き足し待ち時間 | 200 / 300 / 400 / 600 / 800 / 1000 ms | 400ms |
| 早く確定する（即確定） | ON / OFF | ON |
| 読み取りのきびしさ | ゆるい / ふつう / きびしい | ふつう |
| 数字をつかう | ON / OFF | ON |
| 数字だけモード | ON / OFF | OFF |
| 候補を出す時間 | 出さない / 3 / 4 / 5 / 10 秒 | 4秒 |
| ジグザグの判定 | ゆるい / ふつう / きびしい | ふつう |
| 線の太さ | 細 / 中 / 太 | 中 |
| マス目ガイド | なし / 十字 / 十字＋四分割 | 十字 |

#### 画面

| 設定項目 | 選択肢 | 既定 |
| --- | --- | --- |
| 文字サイズ | 標準 / 大 / 特大 | 標準 |
| ログ欄の幅 | 標準(5:2) / せまい(3:1) / ひろい(3:2) / かくす | 標準 |
| 介助者用ボタンを表示 | ON / OFF | ON |
| ハイコントラスト | ON / OFF | OFF |

#### 音

| 設定項目 | 選択肢 | 既定 |
| --- | --- | --- |
| 声で読み上げる | ON / OFF | ON |
| 声を選ぶ | 端末の ja-JP ボイス一覧 | 自動（女性優先） |
| 読み上げ速度 | 遅い / ふつう / 速い | ふつう |
| 声の高さ | 低い / ふつう / 高い | ふつう |
| 効果音 | ON / OFF | ON |
| 指が触れたら小さな音 | ON / OFF | ON |
| 読めないとき「認識できません」と言う | ON / OFF | ON |
| 「けすものが ありません」を言う | ON / OFF | ON |
| はやく書いたら前の声を止める | ON / OFF | OFF |

#### ログ

| 設定項目 | 選択肢 | 既定 |
| --- | --- | --- |
| ログを記録する | ON / OFF | ON |

#### その他

| 設定項目 | 選択肢 | 既定 |
| --- | --- | --- |
| 使用中は画面を消さない | ON / OFF | ON |
| 設定ボタンを長押しで保護 | OFF / ON | OFF |

#### 実行ボタン

| ボタン | 動作 |
| --- | --- |
| 設定を初期値にもどす | 全設定を既定値へ（ログは保持） |
| ログを書き出す（CSV） | `asacom2_log_YYYYMMDD.csv` |
| ログを消去 | 確認のうえ全削除 |

---

## 8. 技術仕様

| 項目 | 内容 |
| --- | --- |
| 実装 | HTML5 + CSS3 + Vanilla JavaScript（ES2020） |
| ファイル構成 | `asa-com2/index.html` 単一ファイル（CSS/JS/テンプレートすべてインライン） |
| 外部依存 | なし（アイコンはインラインSVG、認識テンプレートはJSリテラル） |
| 描画 | Canvas 2D（`devicePixelRatio` 対応） |
| 入力 | Pointer Events（指・Apple Pencil どちらも可） |
| 音声合成 | Web Speech Synthesis API |
| 永続化 | `localStorage`（`asacom2.settings` / `asacom2.log`） |
| ファイル出力 | `Blob` + `<a download>` |
| オフライン | 完全オフライン動作 |
| 対応ブラウザ | iPadOS Safari 16+（主対象）、Chrome 90+、Edge 90+ |
| 想定解像度 | iPad 10.9"/11"（1180×820）を基準、12.9"（1366×1024）／mini（1133×744）に追随 |
| 目標性能 | 1画あたりの認識処理 **20ms 以内**、確定から発音開始まで 150ms 以内 |

### 8.1 データ構造

```js
// 設定
{
  strokeGapMs: 400, fastCommit: true, strictness: 'normal',
  useDigits: true, digitsOnly: false, candidateMs: 4000,
  zigzag: 'normal', penWidth: 'mid', guide: 'cross',
  fontScale: 'normal', logRatio: 'normal', showKeys: true, highContrast: false,
  speak: true, voiceURI: null, rate: 1.0, pitch: 1.05, sound: true,
  cancelPrev: false, sayEmptyDelete: true, sayFail: true, touchTick: true,
  logEnabled: true, keepAwake: true, gearGuard: false
}

// 入力中の行の1文字
{ ch:'カ', score:0.82, alts:[{ch:'力',score:0.6}], ink:[[x,y,…],…] /* 正規化済み筆跡・無い場合 null */ }

// ログ1件
{ id:'a1b2c3', datetime:'2026-09-22T10:15:32+09:00', time:'10:15', type:'text', text:'オチャ ノミタイ。' }
```

### 8.2 アクセシビリティ

- 補助キー・トグルには `aria-label`、ログエリアに `role="log" aria-live="polite"`
- `prefers-reduced-motion` 指定時はアニメーション（カーソル点滅・揺れ）を停止
- フォーカスリング 4px（`#1A73E8`）、スイッチコントロールで補助キーを走査可能
- ピンチズーム・ダブルタップズーム・長押しメニューを無効化
- **VoiceOver は OFF で使う**（ONのままだと画面に指を置いても線が描けないため）。本人への案内は
  アプリ自身の音声で行うので VoiceOver は不要
- 本人が触る面（左ペイン）にはボタンが無いので、誤ってアプリの機能を呼び出すことがない

---

## 9. 検証（開発時の自動テスト）

認識エンジンは `index.html` 内のマーカー（`RECOGNIZER-BEGIN` / `RECOGNIZER-END`）で囲まれた
独立ブロックとして実装し、`test/recognize.test.js`（Node.js）から抽出して自動テストする。

| テスト | 内容 |
| --- | --- |
| 自己一致 | 全テンプレートが自分自身を1位で認識する |
| ゆがみ耐性 | 点の位置に乱数ノイズ・傾き・拡大縮小・平行移動・再サンプリングのゆらぎを加えた筆跡（1字あたり40通り）で 1位正解率を測定 |
| 取り違え一覧 | 誤認識した組み合わせを頻度順に出力（テンプレート改善の材料にする） |
| 即確定の安全性 | 「書き足しの見込みがある」筆跡（例：`ノ`→`メ` の1画目）で即確定しないことを確認 |
| 記号の判定 | 小さな丸＝句点／半濁点、小さな2点＝濁点、1画で続けた濁点、大きな丸（`0`）や小さめの `ー` を記号と誤らないこと |
| ジグザグ | 3回以上の折り返しで削除、`シ` `ツ` `ミ` `サ` などを書いても削除と誤判定しないこと |
| 実画面テスト | Chromium（Playwright）で実際に指の軌跡を再現し、`カ`→濁点→`ガ`、`ハ`→丸→`パ`→丸→`ハ。`（文の確定）、ジグザグ削除、読めないときの「認識できません。」まで通しで確認 |

実行：`node asa-com2/test/recognize.test.js`

**現在の成績**（テンプレートに強いゆがみ＝回転±6°・縦横±18%の伸縮・傾き・手のふるえ・始点終点の欠けを
加えた合成筆跡 2,520 件）：**1位正解 99.2% / 上位3候補に正解 100%**。
取り違えが残るのは `フ`/`7`、`コ`/`ユ`、`ワ`/`リ` など字形がほぼ同じ組で、候補チップ（4.3）と
「数字をつかう」OFF で実用上は回避できる。

---

## 10. テスト後の確認事項（実機テスト後に必ず確認）

> 実機（iPad・本人の指）でのテスト後、以下2点を判断していただきます。**開発側から必ずリマインドします。**

| # | 確認事項 | 判断の材料 |
| --- | --- | --- |
| **T1** | **認識精度が実用に足りるか**。不足する場合は**外部ライブラリ／機械学習モデルの採用**（TensorFlow.js 等）へ切り替える | よく間違える字の一覧、候補チップで訂正した回数。※外部モデルは数MBのダウンロードが必要になり、オフライン性・起動速度とのトレードオフになります |
| **T2** | **スピード（書き足し待ち時間の既定 400ms）が適切か** | 「書き終わる前に確定してしまう」→ 長くする（600〜800ms）／「声が出るまで待たされる」→ 短くする（200〜300ms）。設定画面でその場で変更して比較できます |

---

## 11. 今後の拡張候補（本仕様の対象外）

- **Asa-Com との統合**（設定で「選ぶモード／書くモード」を切り替え、ログを共有）
- 小書き（ャュョッ）・空白・読点への対応
- 本人の筆跡を登録して精度を上げる「手書きをおぼえさせる」機能
- よく使う言葉の辞書・予測変換（「オチャ」→「オチャ ノミタイ」）
- ひらがな・アルファベットへの対応
- 定型文ボタン（書かずに1タップで発話）
- ログのクラウド共有（家族間での閲覧）

---

## 12. 参考：添付画面設計図との対応

| 図中の要素 | 本仕様での対応箇所 |
| --- | --- |
| 左上「ASA-Com2」 | 3.1 ヘッダー |
| 上の帯「アイウエオ」 | 3.2 入力中の行（手書き＋認識文字のペア） |
| 中央の点線の十字と「カ」 | 3.3 手書きキャンバス |
| 右の細い列「アイウエオ」 | 3.5 ログエリア（2/7） |
| 右の注記「ジグザグに書くと1文字前の文字を削除します」 | 4.5 削除ジェスチャ |
